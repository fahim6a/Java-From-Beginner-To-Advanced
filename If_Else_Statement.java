public class If_Else_Statement {
    public static void main(String[] args) {
        // if_else is a conditional statement which is used to
        // take a decision
        int england = 361;
        if(england>359){
            System.out.println("England Win The Ashes Match");
        }
        else {
            System.out.println("Australia Retain The Ashes");
        }

        /* relational operators like
        equals to (==), not equals to (!==)
        less than equals to (<=), greater than equals to (>=)
        less than(<) greater than (>)
        */
    }
}
